import { pgTable, text, serial, timestamp, boolean, numeric } from "drizzle-orm/pg-core";
import { createInsertSchema } from "drizzle-zod";
import { z } from "zod/v4";

export const usersTable = pgTable("users", {
  id: serial("id").primaryKey(),
  email: text("email").notNull().unique(),
  password: text("password").notNull(),
  name: text("name").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow().$onUpdate(() => new Date()),
  autoTradingEnabled: boolean("auto_trading_enabled").notNull().default(false),
  autoTradingConfidenceThreshold: numeric("auto_trading_confidence_threshold", { precision: 5, scale: 2 }).notNull().default("70"),
  autoTradingMaxPositionPct: numeric("auto_trading_max_position_pct", { precision: 5, scale: 2 }).notNull().default("15"),
});

export const insertUserSchema = createInsertSchema(usersTable).omit({ id: true, createdAt: true, updatedAt: true });
export type InsertUser = z.infer<typeof insertUserSchema>;
export type User = typeof usersTable.$inferSelect;
